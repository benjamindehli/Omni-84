.DS_Store
CLAUDE.md

# Paid sample audio + IRs — never commit (regenerated by the converter from the
# private library). Images (assets/images/) and the split manifest (assets/manifest/)
# ARE committed.
/assets/samples/
/assets/ir/

# Build output
build/
cmake-build-*/
out/

# CMake (in-source safety net)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
compile_commands.json
Testing/

# IDE / editors
.vscode/
.idea/
*.xcodeproj/
*.xcworkspace/
.cache/
*.swp
*~

# Packaging output + installers
*.pkg
*.dmg
*-Setup.exe

# Signing secrets — NEVER commit these
*.p8
*.p12
*.cer
*.mobileprovision

# Linux release tarballs
*.tar.gz
packaging/linux/build/

# DecentSampler audio files
/DecentSampler/Samples
/DecentSampler/IR